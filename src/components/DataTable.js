export default function DataTable({ tableRows, values }) {
  console.log("This is the tableRows", tableRows);
  console.log("This is the values", values);

  return (
    <div>
      This is the input data
      <button>clickme</button>
    </div>
  );
}
